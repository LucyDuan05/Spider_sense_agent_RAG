=== BENIGN ===
目标端口53(DNS)。极高包速率(20408 pkt/s)。较高带宽消耗(1367 KB/s)。前向包2个(64B), 后向包2个(204B)。连接极短(0.2ms)，符合扫描探测特征。

=== DDoS ===
目标端口80(HTTP)。低包速率(0.18 pkt/s)。前向包8个(56B), 后向包5个(11607B)。包大小高度变异(均值834B, 标准差2175B)，符合正常交互流量特征。连接持续75秒。流量主要为出站方向，可能存在数据外传。

=== PortScan ===
目标端口21(FTP)。高包速率(3755 pkt/s)。前向包2个(8B), 后向包1个(2B)。包大小极小(均值3B)，可能为扫描探测或控制信令。少量数据推送(PSH标志)。连接极短(0.8ms)，符合扫描探测特征。流量主要为出站方向，可能存在数据外传。

